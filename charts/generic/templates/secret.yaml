{{- range $name, $config := .Values.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Chart.Name }}-{{ $name }}
  labels:
    {{- include "generic.labels" (dict "chartName" $.Chart.Name "name" $name) | nindent 4 }}
type: {{ $config.type | default "Opaque" }}
stringData:
  {{- toYaml $config.stringData | nindent 2 }}
{{- end }}
